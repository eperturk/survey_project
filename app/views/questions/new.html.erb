<h1>Create A New Question for Survey: <%= @survey.title %> </h1>
<%= form_for([@survey, @survey.questions.build]) do |f| %>
	<%= @new_sequence %>
	<%= f.label :text, "Question Text" %>
	<%= f.text_field :text %>
	<%= f.hidden_field :sequence, :value => @new_sequence %>
	<%= f.submit "Submit" %>
<% end %>